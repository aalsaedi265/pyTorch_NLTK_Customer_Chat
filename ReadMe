Installtions
pip3 install nltk
pip3 install numpy
pip3 instal torch



helpful COMMANDS

python3 -m venv .venv source
source .venv/bin/activate
deactivate 

screen gunicorn -w 3 BetterChabBot:app

export PATH=/usr/bin:/bin


Notes
Feed forward nural net
https://chatbotsmagazine.com/contextual-chat-bots-with-tensorflow-4391749d0077

once the option selection is chosen continue with gerneral help
so we can have mutple options apear to be asked
when uploading file
scp -r /mnt/c/Users/16075/Documents/work/BetterChatBot root@45.33.14.128:/BetterChatBot/

the local prot was 5000
if using lenenoe may need to change the port the lenode port which is
45.33.14.128

